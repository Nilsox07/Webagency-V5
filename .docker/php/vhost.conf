<VirtualHost *:80>
    DocumentRoot /var/www/html/public

    # Alles oberhalb von /public ist nicht über den Webserver erreichbar.
    # Das ist keine Bequemlichkeit, sondern Portal-Lastenheft §1.3 — /app, /storage,
    # /migrations und .env dürfen nie ausgeliefert werden.
    <Directory /var/www/html>
        Require all denied
    </Directory>

    <Directory /var/www/html/public>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
